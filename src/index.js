import React from 'react';
import ReactDOM from 'react-dom';
import { MatrixChat } from './components/structures/MatrixChat';

export function renderElement(container, config) {
  ReactDOM.render(
    <span>Test</span>,
    container
  );
}